apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: lh-kubestatemetrics-stage
  namespace: argocd
spec:
  project: linearhealth-stage
  source:
    repoURL: 'git@github.com:linear-health/Gitops.git'
    path: deployments/monitoring/kube-state-metrics
    targetRevision: deploy-stage
    directory:
      recurse: false
      jsonnet: {}
  destination:
    namespace: monitoring
    name: linearhealth-eks-stage
  syncPolicy:   
    syncOptions:
      - CreateNamespace=true