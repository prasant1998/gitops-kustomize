apiVersion: v1
kind: Service
metadata:
  name: servicelhadmin
spec:
  selector:
    app: deploylhadmin
  type: NodePort
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 80
    nodePort: 31310