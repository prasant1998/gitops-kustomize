apiVersion: v1
kind: Service
metadata:
  name: servicelhpatientfrontend
  namespace: default
  labels:
    app: laravel
spec:
  selector:
    app: deploylhpatientfrontend
  type: NodePort
  ports:
  - name: http-patient
    protocol: TCP
    port: 80
    targetPort: 80
    nodePort: 30500